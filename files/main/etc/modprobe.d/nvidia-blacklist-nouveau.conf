# nouveau conflicts with the proprietary 580xx driver; the rpmfusion xorg
# driver's %posttrans normally adds grubby kernel args to blacklist it, but
# that scriptlet is suppressed during the container build (see 04-nvidia.sh).
# bootc manages kernel cmdline itself, so a modprobe blacklist is the right
# mechanism here.
blacklist nouveau
blacklist nvidiafb
